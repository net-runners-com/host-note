{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "go mod download && go mod tidy && go build -o bin/server main.go && go build -o bin/seed cmd/seed/main.go"
  },
  "deploy": {
    "startCommand": "./bin/seed -master-only && ./bin/server",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
